import React from "react";

const Nav = () => {
  console.log("rendering Nav");

  return (
    <nav>
      <img src="" alt="Logo"></img>
      <button>Sign in</button>
    </nav>
  );
};

export default Nav;
